syntax = "proto3";
package datachain.datachain.v1;

import "amino/amino.proto";
import "datachain/datachain/v1/params.proto";
import "gogoproto/gogo.proto";

option go_package = "datachain/x/datachain/types";

// GenesisState defines the datachain module's genesis state.
message GenesisState {
  // params defines all the parameters of the module.
  Params params = 1 [
    (gogoproto.nullable) = false,
    (amino.dont_omitempty) = true
  ];
}
